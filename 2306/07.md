### 오늘 알게 된 것
개발/운영 ssl 인증서 적용 후 동시에 접근이 안되는 현상이 나타났다
이는 JSESSIONID 라는 쿠키 이름의 충돌 때문이였다
세션을 추적하기 위해 클라이언트 쿠키에 JSESSIONID 라는 이름으로 저장하도록 유도하는데
이때, 쿠키에 값을 저장하는 기준은 도메인명과 경로다
개발/운영은 동일 도메인을 사용하고 같은 경로를 사용하여 쿠키에 값이 덮어씌워지게된것였다
해결 방법이 있지만 현재는 이렇게 사용할 경우 하나의 창을 시크릿모드로 사용하거나 두개의 브라우저를 사용하기로 하였다.